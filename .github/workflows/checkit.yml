# This workflow run security tests and send message for orgs into chat
# -679349277
name: Check it!

on:
  issue_comment:
    types:
      - create
  pull_request:
    branches: [ checked ]

jobs:
  security-tests:
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.event.comment.body, '/checkit') }}
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.10" ]

    steps:
    - uses: actions/checkout@v2
    - name: Send hello
      run: echo "Hello"
    - name: Send request  
      uses: appleboy/telegram-action@master
      with:
        to: "-679349277"
        token: "5292735252:AAHbDsG2pWZlYYTlUVOdwE4scVs7c62lz7U"
        message: |
            ❇️ ${{ github.actor }} подал новую заявку на проверку! ❇️            
            Ссылка на pull: https://github.com/${{ github.repository }}/commit/${{github.sha}}
    

