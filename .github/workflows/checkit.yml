# This workflow run security tests and send message for orgs into chat
# -679349277
name: Check it!

on:
  pull_request:
    branches:
      - checked
    types: [opened, reopened]

jobs:
  print-test:
    run: echo 'test'
  security-tests:
    uses: Andrianov-Artemii/ppt-solutions-2022/.github/workflows/checkit.yml@master
